import 'dotenv/config';
import { getSafeDatabaseConfig } from './client.js';

console.log('database migration runner placeholder');
console.log('No database connection will be opened and no SQL will be executed.');
console.log('Database config preview:', getSafeDatabaseConfig());
